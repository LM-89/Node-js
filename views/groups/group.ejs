<%- include('../partials/header', { title: group.name }) %>

    <div class="body-container">
        <% if (group) { %>             
                <div>
                    <a href="/">Home Page</a>
                    <a href="/groups">Groups List</a>
    
                    <h1>Group: <%= group.name %></h1>                
                    <p>Description:<%= group.description %></p>
    
                    <h2>Students in this group:</h2>

                    <ul class="group-students-list">                                
                        <% if (groupStudents.length > 0) { %>
                            <% groupStudents.forEach(student => { %>
                                <li><%= student.name %> <%= student.surname %></li>
                            <% }) %>
                        <% } else { %> 
                            <li>No students assigned..</li>                                        
                        <% } %>
                    </ul>
    
                    <a href="/edit-group/<%= group.id %>"><button>Edit Group Info</button></a>
    
                    <form action="/delete-group" method="POST" style="display:inline;">
                        <input type="hidden" name="groupId" value="<%= group.id %>" />
                        <button type="submit">Delete Group</button>
                    </form>
                </div>        
        <% } else { %>
            <h1>Group not found</h1><a href="/groups">Back to Groups</a>
        <% } %>
    </div>

<%- include('../partials/global-footer') %>